<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobicomp.reminderapp">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReminderApp">
        <activity android:name=".MapsActivityReminder"></activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAJee_6V69Lj2ijcqG9fC0gOZw6v02nepk" />

        <activity android:name=".MapsActivity" />
        <activity android:name=".ReminderEditActivity" />
        <activity android:name=".NewReminderActivity" />
        <activity android:name=".ChangePwActivity" />
        <activity android:name=".ChangeUnActivity" />
        <activity android:name=".ChangeActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".SigninActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>